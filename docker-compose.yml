version: '3.9'

services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    # volumes:
    #   - ./frontend:/app
    environment:
      - VITE_API_URL=http://backend:8000/api